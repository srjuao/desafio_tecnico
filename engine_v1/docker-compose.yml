services:
  w_core:
    build: .
    ports:
      - "4000:4000"
    environment:
      - SECRET_KEY_BASE=${SECRET_KEY_BASE:-$(openssl rand -base64 48)}
      - DATABASE_PATH=/data/w_core.db
      - PHX_HOST=localhost
    volumes:
      - ./data:/data
    restart: always

volumes:
  data:
